[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cauco-agents"
version = "0.1.0"
description = "Deterministic agent contracts and registry for Cauco"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/cauco_agents"]
