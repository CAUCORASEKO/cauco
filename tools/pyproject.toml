[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cauco-tools"
version = "0.1.0"
description = "Permission-aware deterministic tools for Cauco"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/cauco_tools"]
